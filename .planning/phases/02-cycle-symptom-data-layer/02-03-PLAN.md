---
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - api/src/services/cycle.service.ts
  - api/src/controllers/cycle.controller.ts
  - api/src/routes/cycle.routes.ts
  - api/src/__tests__/stats.test.ts
autonomous: true
---

# Plan 2.3: Cycle Stats & Integration Verification

## Goal
Implement aggregate statistics calculator for cycle insights (average cycle length, period length variation, most common symptoms) which serves as the core internal calculation pipeline before AI ingestion. Improve overall Jest tests coverage.

## Tasks

<task>
<read_first>
- api/src/services/cycle.service.ts
</read_first>
<action>
Export `getStats` method from `CycleService`.
Taking `userId`.
Calculates via raw calculation loops or Prisma DB aggregation:
- Average cycleLength (filtering completed cycles).
- Average periodLength (computing `endDate - startDate` for records).
- Most common symptoms (via joined `.symptoms` count aggregations).
- Cycle regularity score. Simple logic: compute standard deviation of `cycleLength` difference across the last 6 cycles. The inverse maps to "score". E.g., `(28 - average)` variances mapping to numeric representations or basic variation coefficient if complicated. For now, keep it a basic `number` mapping sum differences.
Returns the aggregated JSON object.
</action>
<acceptance_criteria>
`cycle.service.ts` contains `getStats(userId)` parsing data.
</acceptance_criteria>
</task>

<task>
<read_first>
- api/src/routes/cycle.routes.ts
</read_first>
<action>
Expose `GET /api/cycles/stats` in `cycle.routes.ts`. Note: Ensure this is routed BEFORE the `/:id` GET parameter wildcard map, to prevent Express from treating "stats" as an `id`.
Connect to `cycle.controller.ts` > `getStats`.
</action>
<acceptance_criteria>
`cycle.routes.ts` defines `/stats` strictly above variable parameter lookups.
</acceptance_criteria>
</task>

<task>
<read_first>
- api/src/__tests__/helpers/app.helpers.ts
</read_first>
<action>
Create `api/src/__tests__/stats.test.ts` focusing uniquely on math logic checks.
Because Prisma isn't available for DB queries, heavily mock `Prisma` return structures manually inside tests asserting if Service executes the aggregate equations correctly for 3 fake cycles with varied start dates and lengths.
</action>
<acceptance_criteria>
`stats.test.ts` tests mock variables representing 3 DB rows and confirm math outputs equal expected averages.
</acceptance_criteria>
</task>
