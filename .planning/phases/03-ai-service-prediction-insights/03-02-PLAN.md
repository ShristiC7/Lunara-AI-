---
wave: 1
depends_on: ["03-01"]
files_modified:
  - ai/app/utils/openai_client.py
  - ai/app/schemas/symptom_schemas.py
  - ai/app/services/symptom_analyzer.py
  - ai/app/main.py
autonomous: true
---

# Plan 3.2: OpenAI GPT-4o Symptom Analysis Service

## Goal
Implement structured symptom analysis using OpenAI's GPT-4o, ensuring high resilience via retries and strict output validation.

## Tasks

<task>
<read_first>
- ai/requirements.txt
</read_first>
<action>
Create `ai/app/utils/openai_client.py`.
- Initialize `AsyncOpenAI`.
- Implement `analyze_symptoms_with_retries` using `tenacity`.
- Handle `RateLimitError` and `APIStatusError` with exponential backoff.
</action>
<acceptance_criteria>
Client is asynchronous and includes tenacity-based retry logic.
</acceptance_criteria>
</task>

<task>
<read_first>
- .planning/phases/03-ai-service-prediction-insights/03-RESEARCH.md
</read_first>
<action>
Create `ai/app/schemas/symptom_schemas.py`:
- `SymptomEntry`: `date`, `mood`, `energy`, `flow`, `pain`, `notes` (validated integers/strings).
- `SymptomAnalysisRequest`: `userId`, `symptoms` (List[SymptomEntry]).
- `SymptomAnalysisResponse`: `pattern_summary` (str), `notable_symptoms` (List[str]), `recommendation` (str), `generated_at` (datetime).
</action>
<acceptance_criteria>
Schemas strictly typed to ensure clean interface with OpenAI and Node.js.
</acceptance_criteria>
</task>

<task>
<read_first>
- ai/app/schemas/symptom_schemas.py
</read_first>
<action>
Create `ai/app/services/symptom_analyzer.py`.
- Build the system prompt: "You are an expert women's health AI. Analyze the following symptom data..."
- Sanitize `notes` field (strip special characters).
- Use OpenAI's `response_format: { "type": "json_object" }`.
- Parse response into `SymptomAnalysisResponse`.
</action>
<acceptance_criteria>
Analyzer produces structured insights and prevents prompt injection via note sanitization.
</acceptance_criteria>
</task>

<task>
<read_first>
- ai/app/main.py
</read_first>
<action>
Update `ai/app/main.py`:
- Add `POST /analyze/symptoms` endpoint.
- Connect it to `symptom_analyzer.py`.
</action>
<acceptance_criteria>
FastAPI exposes the analysis endpoint.
</acceptance_criteria>
</task>
