---
wave: 1
depends_on: []
files_modified:
  - api/src/templates/report.hbs
  - api/src/utils/pdf.ts
  - api/package.json
autonomous: true
---

# Plan 5.1: Handlebars Templates + PDF Generation

## Goal
Implement the core PDF generation engine using Handlebars templates and Puppeteer, optimized for healthy document rendering.

## Tasks

<task>
<read_first>
- api/package.json
</read_first>
<action>
Verify `puppeteer` and `handlebars` are installed.
Install if missing.
</action>
<acceptance_criteria>
`puppeteer` and `handlebars` are in dependencies.
</acceptance_criteria>
</task>

<task>
<read_first>
- .planning/phases/05-pdf-reports-email-delivery/05-RESEARCH.md
</read_first>
<action>
Create `api/src/templates/report.hbs`.
Design a premium HTML layout:
- Header: Lunara AI Logo/Name, Report Date, User ID placeholder.
- Summary Section: Average cycle length, period length.
- Cycle History Table: Date ranges, lengths.
- Symptom Insights: List of most common symptoms.
- AI Logic: Prediction for next cycle.
- Footer: Disclaimer (Not a medical diagnosis).
- Inline CSS for print optimization (@media print).
</action>
<acceptance_criteria>
Handlebars template covers all required report sections with premium styling.
</acceptance_criteria>
</task>

<task>
<read_first>
- api/src/templates/report.hbs
</read_first>
<action>
Create `api/src/utils/pdf.ts`.
Implement `PdfGenerator` class:
- `generate(data)`: 
  - Compiles Handlebars template.
  - Launches Puppeteer with safe sandbox flags.
  - Sets content and prints to PDF buffer.
  - Handles errors and ensures browser closure.
</action>
<acceptance_criteria>
`generate` method returns a valid PDF Buffer and cleans up Puppeteer instances.
</acceptance_criteria>
</task>
