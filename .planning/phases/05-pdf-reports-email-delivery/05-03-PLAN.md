---
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - api/src/utils/email.ts
  - api/src/templates/email-report.hbs
  - api/src/templates/email-reset.hbs
  - api/src/queues/email.queue.ts
  - api/src/workers/email.worker.ts
  - api/src/index.ts
autonomous: true
---

# Plan 5.3: Email Service + Templates

## Goal
Implement the resilient email delivery system for health reports and transactional messages (password reset).

## Tasks

<task>
<read_first>
- api/package.json
</read_first>
<action>
Verify `nodemailer` is installed.
Create `api/src/utils/email.ts`.
Implement `EmailService`:
- `send(to, subject, html, attachments)`: Sends via SMTP transport.
- Uses env: `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM`.
</action>
<acceptance_criteria>
Email service provides a clean abstraction for sending rich emails with attachments.
</acceptance_criteria>
</task>

<task>
<read_first>
- .planning/phases/05-pdf-reports-email-delivery/05-RESEARCH.md
</read_first>
<action>
Create Handlebars templates:
- `api/src/templates/email-report.hbs`: Branded report notification.
- `api/src/templates/email-reset.hbs`: Password reset link.
</action>
<acceptance_criteria>
Templates are consistent with Lunara AI branding.
</acceptance_criteria>
</task>

<task>
<read_first>
- api/src/queues/base.queue.ts
</read_first>
<action>
Create `api/src/queues/email.queue.ts`.
Create `api/src/workers/email.worker.ts`.
Process `report-delivery` and `password-reset` jobs.
- Handle attachment fetching (from S3 or Buffer).
- Log success/failure.
Initialize in `index.ts`.
</action>
<acceptance_criteria>
Email jobs are processed asynchronously with retries on SMTP errors.
</acceptance_criteria>
</task>

<task>
<read_first>
- api/src/controllers/auth.controller.ts
</read_first>
<action>
Fully implement the `forgotPassword` flow:
- Generate token.
- Queue `password-reset` job.
</action>
<acceptance_criteria>
Password reset cycle is complete from trigger to email receipt.
</acceptance_criteria>
</task>
