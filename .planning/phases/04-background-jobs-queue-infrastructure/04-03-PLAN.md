---
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - api/src/workers/report.worker.ts
  - api/src/controllers/ai.controller.ts
  - api/src/__tests__/jobs.test.ts
autonomous: true
---

# Plan 4.3: Report Generation Worker (Stub) + Infra Verification

## Goal
Establish the report generation infrastructure with a stub worker to verify queue functionality and complete the Phase 4 verification.

## Tasks

<task>
<read_first>
- api/src/queues/report.queue.ts
</read_first>
<action>
Create `api/src/workers/report.worker.ts`.
Implement `reportQueue.process('generate-pdf', ...)` as a stub:
- Log job receipt.
- Simulate progress 10% → 50% → 100% with `job.progress()`.
- Return dummy data.
- Attach failed/completed handlers same as AI worker.
</action>
<acceptance_criteria>
Report worker infrastructure is ready for the future Puppeteer implementation.
</acceptance_criteria>
</task>

<task>
<read_first>
- api/src/controllers/ai.controller.ts
</read_first>
<action>
Update `triggerAnalysis` in `ai.controller.ts` to return `jobId` and 202 Accepted.
</action>
<acceptance_criteria>
Controller correctly reflects the async nature of analysis.
</acceptance_criteria>
</task>

<task>
<read_first>
- api/src/__tests__/helpers/app.helpers.ts
</read_first>
<action>
Create `api/src/__tests__/jobs.test.ts`.
- Test `GET /api/jobs/:id` for valid and invalid jobs.
- Test `POST /api/insights/trigger` returns a `jobId`.
- Mock Bull methods to avoid Redis connection in tests.
</action>
<acceptance_criteria>
End-to-end integration of the job submission path is verified.
</acceptance_criteria>
</task>
