---
wave: 1
depends_on: []
files_modified:
  - api/src/queues/base.queue.ts
  - api/src/queues/ai.queue.ts
  - api/src/queues/report.queue.ts
  - api/src/controllers/job.controller.ts
  - api/src/routes/job.routes.ts
  - api/src/app.ts
autonomous: true
---

# Plan 4.1: Queue Definitions + Configuration

## Goal
Set up the Bull queue infrastructure for AI inference and PDF generation, and expose a status endpoint for monitoring jobs.

## Tasks

<task>
<read_first>
- api/package.json
</read_first>
<action>
Verify/Install `bull`. (It should be in the stack from Phase 1 initialization).
</action>
<acceptance_criteria>
`bull` is present in `api/package.json`.
</acceptance_criteria>
</task>

<task>
<read_first>
- .planning/phases/04-background-jobs-queue-infrastructure/04-RESEARCH.md
</read_first>
<action>
Create `api/src/queues/ai.queue.ts`.
Create `api/src/queues/report.queue.ts`.
Export singleton instances of the queues with appropriate options:
- `attempts`: 5 for AI, 3 for Report.
- `backoff`: exponential for AI, fixed for Report.
- `removeOnComplete`: 100 limit.
- `removeOnFail`: 50 limit.
Use `process.env.REDIS_URL` for connection.
</action>
<acceptance_criteria>
Queues are singleton instances with documented resilience settings.
</acceptance_criteria>
</task>

<task>
<read_first>
- api/src/queues/ai.queue.ts
- api/src/queues/report.queue.ts
</read_first>
<action>
Create `api/src/controllers/job.controller.ts`.
Implement `getJobStatus`:
- Take `jobId`.
- Search `aiQueue` then `reportQueue`.
- Return status data `{ id, state, progress, result, failedReason }`.
</action>
<acceptance_criteria>
Controller can correctly identify and status jobs across multiple queues.
</acceptance_criteria>
</task>

<task>
<read_first>
- api/src/app.ts
</read_first>
<action>
Create `api/src/routes/job.routes.ts` mounted at `/api/jobs`.
Register `GET /:id` to `jobController.getJobStatus`.
Mount in `app.ts`.
</action>
<acceptance_criteria>
Job status endpoint is reachable via authenticated requests.
</acceptance_criteria>
</task>
