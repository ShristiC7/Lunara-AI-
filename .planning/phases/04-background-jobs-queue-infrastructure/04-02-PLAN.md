---
wave: 1
depends_on: ["04-01"]
files_modified:
  - api/src/workers/ai.worker.ts
  - api/src/services/ai.service.ts
  - api/src/index.ts
  - api/src/app.ts
autonomous: true
---

# Plan 4.2: AI Inference Worker

## Goal
Implement the worker logic for processing symptom analysis asynchronously, improving API responsiveness and reliability.

## Tasks

<task>
<read_first>
- api/src/services/ai.service.ts
- api/src/queues/ai.queue.ts
</read_first>
<action>
Modify `api/src/services/ai.service.ts`:
- Change `triggerSymptomAnalysis` to JUST queue the job and return the `jobId`. 
- Implementation: `const job = await aiQueue.add('analyze-symptoms', { userId }, { ...options }); return job.id;`.
</action>
<acceptance_criteria>
`triggerSymptomAnalysis` returns a `jobId` instead of performing the work synchronously.
</acceptance_criteria>
</task>

<task>
<read_first>
- api/src/services/ai.service.ts
</read_first>
<action>
Create `api/src/workers/ai.worker.ts`.
Implement `aiQueue.process('analyze-symptoms', ...)`:
- Fetch user symptoms.
- Call FastAPI.
- Store results in `AiInsight` table.
- Use `logger` for progress and completion events.
</action>
<acceptance_criteria>
Worker logic correctly handles the full AI analysis lifecycle independently.
</acceptance_criteria>
</task>

<task>
<read_first>
- api/src/workers/ai.worker.ts
</read_first>
<action>
Implement error handlers in `ai.worker.ts`:
- `aiQueue.on('failed', (job, err) => { ... })`: Log error, update insight status if available, and write to `AuditLog`.
- `aiQueue.on('completed', ...)`: Handle cleanup if not handled by `removeOnComplete`.
</action>
<acceptance_criteria>
Failures are auditable and logged with context.
</acceptance_criteria>
</task>

<task>
<read_first>
- api/src/index.ts
</read_first>
<action>
Initialize workers in `src/index.ts` only when not in test environment.
Add graceful shutdown for queues in the shutdown sequence.
</action>
<acceptance_criteria>
Workers start automatically with the server but stay silent during unit tests. Shutdown is clean.
</acceptance_criteria>
</task>
